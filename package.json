{
	"name": "skill-icons",
	"version": "1.0.0",
	"main": "index.js",
	"license": "MIT",
	"scripts": {
		"dev": "node build.js && wrangler dev --remote",
		"build": "node build.js",
		"deploy": "node build.js && wrangler publish"
	},
	"devDependencies": {
		"@cloudflare/wrangler": "^1.19.8",
		"prettier": "^2.5.1"
	},
	"dependencies": {
		"wrangler": "^3.0.1"
	}
}
